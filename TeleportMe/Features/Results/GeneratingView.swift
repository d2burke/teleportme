import SwiftUI

// MARK: - GeneratingView (DEPRECATED — use ExplorationGeneratingStepView instead)
//
// This view is no longer used. Both onboarding and new-exploration flows now use
// ExplorationGeneratingStepView which calls explorationService.generateExploration()
// directly with explicit parameters and proper session refresh.
//
// Kept only to avoid Xcode project file churn. Safe to delete entirely.
